# This workflow updates image paths in Markdown files to GitHub URLs

name: Update Image Paths

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # This workflow contains a single job called "convert-image-path"
  convert-image-path:
    runs-on: ubuntu-latest

    steps:
      # Checks out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Updates image paths in Markdown files
      - name: Update image paths in Markdown
        run: |
          set -e  # Exit immediately if a command exits with a non-zero status

          for file in */*/*.md; do
            echo "------------------------------------------------------------------------------------------"
          
            # Create a backup of the original file
            image_path_changed_file="${file%.md}_image_path_changed.md"
            cp "$file" "$image_path_changed_file"
          
            # Update image paths
            if sed -i.bak -E "s|(\!\[.*\]\()\./images/(.*\))|\1https://raw.githubusercontent.com/${GITHUB_REPOSITORY}/main/$(dirname "$file")/images/\2|g" "$image_path_changed_file"; then
                echo "Processing \"$image_path_changed_file\""
                echo "Directory: $(dirname \"$image_path_changed_file\")"
                echo "Updated image paths in \"$image_path_changed_file\""
            else
                echo "No image paths found in \"$image_path_changed_file\""
            fi

            # 이미지 경로를 변경
            if ! git diff --quiet "$image_path_changed_file"; then
                git add "$image_path_changed_file"
                echo "Added: \"$image_path_changed_file\""
            else
                echo "No changes detected in \"$image_path_changed_file\""
            fi
          done

      # Commits changes if there are any
      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git status
          if git commit -m "Update image paths to GitHub URLs"; then
              echo "Changes committed successfully."
              git push
          else
              echo "No changes to commit."
          fi